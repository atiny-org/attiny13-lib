# ATiny13-Lib
An ATiny13 library containing tools and drivers to operate core peripherals in package. This library is built on top of (and therefore assumes the use of) [avr-libc](https://www.nongnu.org/avr-libc/).
